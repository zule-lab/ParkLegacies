targets_models <- c(
  
  tar_target(
    generative_data,
    data_generative()
  )#,
  
  #tar_target(
  #  generative_model,
  #  model_generative()
  #),
  #
  #tar_target(
  #  data_model,
  #  model_data()
  #)
  
)